class CardGame:


  def check_for_ace(self, card):
    # should be if card.value == 1:
    if card.value = 1:
      return True
     # missing :
    else
      return False
   

  # spelling error, should be def. Also missing a comma between card1, card2
  dif highest_card(self, card1 card2):
  # indentation error 
  if card1.value > card2.value:
    # should be card1 not card
    return card 
  else:
    return card2
  

# indentation error, function should be inside class
def cards_total(self, cards):
  # should be set to 0 (total = 0)
  total
  for card in cards:
    total += card.value
    # indentation error, return should be outside of the loop. 
    # Change the output to a formatted string
    return "You have a total of" + total